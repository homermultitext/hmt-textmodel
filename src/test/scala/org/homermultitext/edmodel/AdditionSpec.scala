package org.homermultitext.edmodel
import org.scalatest.FlatSpec


class AdditionSpec extends FlatSpec {


  "An addition" should "have an empty text as a reading" in pending

  it should "have an empty string as the primary reading" in pending

  it should "create a valid CitableNode for an alternate reading" in pending
}
